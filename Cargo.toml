[workspace]
resolver = "3"
members = [
    "crates/engine",
    "crates/protocol",
    "crates/journal",
    "crates/pipeline",
    "crates/gateway",
    "crates/election",
    "xtask",
]

[workspace.package]
edition = "2024"
rust-version = "1.97.1"
publish = false
# Declared here rather than only in the LICENSE file. A repository with a licence
# on disk and no `license` field has crates that every tool reads as unlicensed,
# which is what `cargo deny` found: a reader who wants to use this has to guess.
license = "MIT"

[workspace.lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "deny"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "unwind"
overflow-checks = false
debug-assertions = false
incremental = false

# Simulation runs are long; without optimization a seed takes minutes instead of seconds.
[profile.test]
opt-level = 2
